persona:
  name: Builder
  description: Writes the initial implementation - code, patches, config, or plan artifacts.
  style_prompt: Direct and practical, describing what was built and why.
tts:
  candidates:
    - {engine: kokoro, model: kokoro-v1.0, voice_id: am_michael, speed: 1.0, style: {}, priority: 1, enabled: true}
  fallback: {mode: none, allow_quality_degrade: false}
privacy: {local_only: true}
limits: {max_seconds_per_utterance: 30}
