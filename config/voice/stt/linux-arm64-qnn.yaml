function: stt
profile: linux-arm64-qnn
files: []
fallback_to: linux-arm64-cpu
notes: No sherpa-onnx Whisper QNN build exists upstream as of this writing - verified against sherpa-onnx's
  real GitHub releases, which publish QNN-tagged assets only for a different model family ('sense-voice',
  not Whisper). This is a real gap in what's published upstream, not a search failure. Falls back to the
  arch's *64-cpu profile per Section 16.4's own fallback rule, rather than a fabricated pin.
