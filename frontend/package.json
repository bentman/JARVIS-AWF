{
  "name": "awf-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "cli",
    "gui"
  ],
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run build && npm --workspace awf-gui run start",
    "test": "npm run test --workspaces"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
